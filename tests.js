// Should throw an error if the input is asymetric

// Rendering functions are not tested
// Factory functions also not

// iteration count must be the same as move count

// todo support another number of colors
